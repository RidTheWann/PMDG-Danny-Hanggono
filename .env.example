# Database connection string (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Google Sheets API URL (from Google Apps Script deployment)
GOOGLE_SHEETS_URL=https://script.google.com/macros/s/your-script-id/exec

# Google Sheets Spreadsheet ID
SPREADSHEET_ID=your-spreadsheet-id

# Google Service Account credentials (JSON format)
# IMPORTANT: In production, use Vercel Environment Variables or similar secure storage
# DO NOT commit actual credentials to version control
GOOGLE_CREDENTIALS={ "type": "service_account", ... }